#!/usr/bin/env sh
set -e
set -u

# Since this runs under Travis, we don't use shellfire, yet.

# if core_variable_isSet TRAVIS_CI ...
# 	git submodule update --init --recursive
# fi

rm -rf ./output
tools/fatten/fatten --force --repository-path tools/fatten --output-path ./output -- fatten
output/usr/bin/fatten --verbose 3 --force --output-path ./output -- paths.d-helper
